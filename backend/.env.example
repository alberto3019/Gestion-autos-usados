# Base de Datos - PostgreSQL (Supabase)
# Usa la URL del Session Pooler para mejor compatibilidad
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[TU_PASSWORD]@db.[PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true&schema=public"

# JWT - Autenticación
# Genera secretos seguros con: openssl rand -base64 32
JWT_SECRET="tu-secreto-super-seguro-cambiar-en-produccion"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="tu-refresh-token-secreto-cambiar-en-produccion"
JWT_REFRESH_EXPIRES_IN="7d"

# Servidor
PORT=3000
NODE_ENV=development

# URLs
FRONTEND_URL="http://localhost:5173"
API_URL="http://localhost:3000"

# Uploads (opcional - si usas almacenamiento local)
UPLOAD_DIR="./uploads"
UPLOAD_BASE_URL="/uploads"

# AWS S3 (opcional - si usas S3 para almacenar imágenes)
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
# AWS_REGION="us-east-1"
# AWS_S3_BUCKET=""

# Email SMTP (opcional - para recuperación de contraseña y notificaciones)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER=""
# SMTP_PASS=""
# SMTP_FROM=""
